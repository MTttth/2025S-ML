"""
把“split_digits”切出的每个子图送入训练好的分类器，得到 0–9 标签，并按从左到右拼接成最终读数字符串。
输入：
- 原始电表图或 ROI 图（用于可选可视化）
- 检测模型权重 (best.pt)
- 分类模型权重 (classifier_best.pth)
- 切分函数或中间切分好的 digits/*.png
输出：将每个子图分类后拼接成的数字字符串，或可视化结果（如标注原图上的数字读数）
"""